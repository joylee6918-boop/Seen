import XCTest

final class ShiguangTests: XCTestCase {
    func testTestBundleLoads() throws {
        XCTAssertTrue(true)
    }

    func testPerformanceExample() throws {
        measure {
            _ = UUID().uuidString
        }
    }
}
